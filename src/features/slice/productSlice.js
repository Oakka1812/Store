import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    products : [],
}

export const productSlice = ({
    name : 'product',
    initialState,
    reducer : {

    }
})

export default productSlice.reducer